/**
 * [INPUT]: 无依赖 — 背景渐变已由 CSS #bg-root 处理
 * [OUTPUT]: GradientBg 组件（空渲染，保留接口兼容）
 * [POS]: components 的背景占位，被 App 消费
 * [PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
 */

export default function GradientBg() {
  return null
}
